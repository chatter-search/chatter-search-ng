image: "node:5.6.0"

stages:
  - test
  - deploy

cache:
  name: ${CI_BUILD_REF_NAME}
  paths:
    - node_modules/
  group: sharedcache

before_script:
  - npm install --production

test:
  stage: test
  script:
    - npm test

deploy:
  stage: deploy
  script:
    - npm run deploy
