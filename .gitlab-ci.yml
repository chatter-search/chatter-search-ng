image: "node:5.6.0"

stages:
  - install
  - test
  - deploy

install:
  stage: install
  script:
    - npm install
  cache:
    key: ${CI_PROJECT_ID}
    paths:
      - node_modules/

test:
  stage: test
  script:
    - npm test

deploy:
  stage: deploy
  script:
    - npm run deploy
